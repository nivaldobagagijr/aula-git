# aula-git


git init
git add README.md
git commit -m "first commit"

* Vinculei o diretório local com o diretório remoto

`git remote add origin https://github.com/nivaldobagagijr/aula-git.git`

* Postei o conteúdo do diretório local para o github

`git push -u origin master`